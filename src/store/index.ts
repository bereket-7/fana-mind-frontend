import { defineStore } from "pinia";

export const useStore = defineStore("main", {
  state: () => ({
    // Add state here
  }),
  getters: {
    // Add getters here
  },
  actions: {
    // Add actions here
  },
});
